 <el-row :gutter="20">
      <el-col :span="24">
        <div class="info-item">
          <div class="label">选择客户：</div>
          <div class="value">
            <el-select
              :model-value="customerId"
              placeholder="请选择客户"
              filterable
              style="width: 100%"
              @change="emitCustomer"
            >
              <el-option
                v-for="customer in customerList"
                :key="customer.id"
                :label="customer.name + ' - ' + customer.phone"
                :value="customer.id"
              />
            </el-select>
          </div>
          <el-button type="primary" link @click="showAddDialog">新增客户</el-button>
        </div>
      </el-col>
    </el-row>